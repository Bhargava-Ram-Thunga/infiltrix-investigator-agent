# Pytest configuration for the Investigator Agent module.
# Run from this directory: `pytest tests/` (or just `pytest`).
[pytest]
testpaths = tests
addopts = -q --strict-markers
# Fail loudly on accidental test-name typos (test discovery defaults).
python_files = test_*.py
python_classes = Test*
python_functions = test_*
